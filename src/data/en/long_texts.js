const summary = `Hello! My name is Tima, a Go developer at Wildberries.
My job is to build high-performance backend systems with a strong focus on code organization and architectural clarity.
In my daily work, I use <i>Go</i>, <i>PostgreSQL</i>, <i>Kafka</i> и <i>Docker</i>.`

const summaryVue = `I have experience with <span class="w3-text-teal">Vue</span> and am
ready to take on frontend tasks when needed.`

const runText = `In my free time, I go running <span class="w3-text-teal w3-small">(Strava)</span> and read.`

export default {
    summary,
    summaryVue,
    runText
}
